[pytest]
markers =
    unit: mark a test as a unit test.

addopts = -vs -rf --html=reports/report.html --self-contained-html
testpaths = tests

log_level = INFO
log_cli = true
log_cli_level = INFO
log_format = %(asctime)s - %(levelname)s - %(message)s
log_date_format = %H:%M:%S



